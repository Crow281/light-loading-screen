/**
 * Activates the app.
 */
//Create a simple header with the string, "hello world".
const header = document.createElement("h1");
header.innerText = "Hello World";

//Add it to the document.
document.body.appendChild(header);
